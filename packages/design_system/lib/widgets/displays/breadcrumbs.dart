import 'package:design_system/lib.dart';

class Breadcrumbs extends AppStatelessWidget {
  const Breadcrumbs({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

