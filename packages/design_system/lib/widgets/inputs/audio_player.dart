import 'package:design_system/lib.dart';

class AudioPlayer extends AppStatefulWidget {
  const AudioPlayer({super.key});

  @override
  AppState<AudioPlayer> createState() => _AudioPlayerState();
}

class _AudioPlayerState extends AppState<AudioPlayer> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
